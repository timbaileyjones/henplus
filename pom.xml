<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>henplus</groupId>
  <artifactId>henplus</artifactId>
  <version>0.9.9-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>HenPlus</name>
  <description>A SQL shell that can handle multiple sessions in parallel.</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>1.8</maven.compiler.source>
    <maven.compiler.target>1.8</maven.compiler.target>
    <henplus.title>Friendly Fire</henplus.title>
  </properties>

  <dependencies>
    <dependency>
      <groupId>commons-cli</groupId>
      <artifactId>commons-cli</artifactId>
      <version>1.11.0</version>
    </dependency>
    <dependency>
      <groupId>org.jline</groupId>
      <artifactId>jline-reader</artifactId>
      <version>3.22.0</version>
    </dependency>
    <dependency>
      <groupId>org.jline</groupId>
      <artifactId>jline-terminal</artifactId>
      <version>3.22.0</version>
    </dependency>

    <!-- JDBC drivers: runtime-only (loaded via DriverManager/ServiceLoader,
         never referenced from our own source), so they never affect
         compilation. Copied into the gitignored drivers/ directory (see the
         copy-drivers execution below), never into the git-tracked lib/ -
         several of these (ojdbc8 in particular, under Oracle's Free Use
         Terms and Conditions) restrict redistribution, so nothing here
         should ever end up committed. -->
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <version>42.7.13</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <version>26.7.0</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.mariadb.jdbc</groupId>
      <artifactId>mariadb-java-client</artifactId>
      <version>3.5.10</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.microsoft.sqlserver</groupId>
      <artifactId>mssql-jdbc</artifactId>
      <version>13.4.0.jre8</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.oracle.database.jdbc</groupId>
      <artifactId>ojdbc8</artifactId>
      <version>23.26.3.0.0</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>com.ibm.db2</groupId>
      <artifactId>jcc</artifactId>
      <version>12.1.5.0</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.xerial</groupId>
      <artifactId>sqlite-jdbc</artifactId>
      <version>3.53.2.1</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.14.4</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.platform</groupId>
      <artifactId>junit-platform-launcher</artifactId>
      <version>1.14.4</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <!-- mirrors the Ant build's output layout (build/classes, build/henplus.jar)
         so bin/henplus, which looks for classes/jars there when not installed,
         keeps working unmodified. -->
    <directory>build</directory>
    <outputDirectory>build/classes</outputDirectory>
    <finalName>henplus</finalName>

    <resources>
      <resource>
        <directory>src/main/resources-templates</directory>
        <filtering>true</filtering>
        <excludes>
          <exclude>**/*</exclude>
        </excludes>
      </resource>
    </resources>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.15.0</version>
      </plugin>

      <!-- Unit tests only (*Test.java) - no network/database access, run on
           every "mvn test"/"mvn package". End-to-end tests (*IT.java) are
           explicitly excluded here and handled by failsafe instead, since
           they need real database credentials and shouldn't block a normal
           build. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.6</version>
        <configuration>
          <excludes>
            <exclude>**/*IT.java</exclude>
          </excludes>
        </configuration>
      </plugin>

      <!-- End-to-end tests (*IT.java) against real databases. Only run on
           "mvn verify" (or "mvn failsafe:integration-test"), never on a
           plain "mvn package" - they need credentials from
           ~/.config/henplus/e2e-connectstrings.properties, which is
           deliberately not part of this repo. See
           henplus.e2e.E2EConfig/E2EConnectionsIT. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>3.5.6</version>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- Filters src/main/resources-templates/henplus/Version.java (Ant-style
           @token@ placeholders) into src/main/java before compilation, replacing
           the old build.xml "Version.java.in" copy+filter target. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-resources-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <id>generate-version-java</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>copy-resources</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.basedir}/src/main/java</outputDirectory>
              <resources>
                <resource>
                  <directory>src/main/resources-templates</directory>
                  <filtering>true</filtering>
                </resource>
              </resources>
              <delimiters>
                <delimiter>@</delimiter>
              </delimiters>
              <useDefaultDelimiters>false</useDefaultDelimiters>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.5.1</version>
        <configuration>
          <archive>
            <manifestFile>manifest.txt</manifestFile>
          </archive>
        </configuration>
      </plugin>

      <!-- Keeps lib/*.jar (still read directly by bin/henplus) in sync with
           the versions declared above, for the non-"mvn install" dev flow.
           Restricted to compile scope so JDBC drivers never land in the
           git-tracked lib/ - see copy-drivers below for those. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.11.0</version>
        <executions>
          <execution>
            <id>copy-dependencies-to-lib</id>
            <phase>package</phase>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.basedir}/lib</outputDirectory>
              <includeScope>compile</includeScope>
            </configuration>
          </execution>

          <!-- JDBC drivers land in the gitignored drivers/ directory, never
               in lib/. Point bin/henplus at them with an extra classpath
               entry, or copy the one(s) you need into ~/.henplus/lib. -->
          <execution>
            <id>copy-drivers</id>
            <phase>package</phase>
            <goals>
              <goal>copy-dependencies</goal>
            </goals>
            <configuration>
              <outputDirectory>${project.basedir}/drivers</outputDirectory>
              <excludeScope>compile</excludeScope>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
